import React from 'react';

const CoolButton = (props) => {
    const { className, isSmall, isDanger, isSuccess }
    if ()
  return (
    <div>
      <button class={props.className}>{props.children}</button>
    </div>
  )
}

export default CoolButton;