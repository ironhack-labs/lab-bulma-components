import React from 'react';
import ReactDOM from 'react-dom';   //import only here to use ReactDom.render method below
import App from './App';
  //import only once in root to use in all components instead of importing in each component

                   
   



ReactDOM.render(<App />, document.getElementById('root'));
